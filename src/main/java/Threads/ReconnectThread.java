package Threads;

public class ReconnectThread {

    static class rejoinThread implements Runnable{
        @Override
        public void run(){

        }
    }

    /*
        ReconnectThread.rejoinThread RejoinThread = new ReconnectThread.rejoinThread();
        Thread reThread = new Thread(reThread);
        reThread.start();
     */
}
